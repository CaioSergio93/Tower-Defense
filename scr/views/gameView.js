// js/views/gameView.js

class GameView {
    constructor() {
        // Inicialização da visualização do jogo
        this.gameContainer = document.getElementById('game-container');
    }

    renderMap(mapData) {
        // Renderização do mapa do jogo
        // Exemplo: criar elementos HTML para representar o mapa
    }

    renderTower(towerData) {
        // Renderização de uma torre
        // Exemplo: criar um elemento HTML para representar a torre
    }

    renderEnemy(enemyData) {
        // Renderização de um inimigo
        // Exemplo: criar um elemento HTML para representar o inimigo
    }

    // Outros métodos de renderização aqui
}

// Exportando a classe para uso em outros arquivos
export default GameView;
